# B5 shared workflow handoff — September 26, 2026

B5 shared-workflow engineering is complete within the documented configurations and guarded-stop behavior. The current B5 source is distinct from the retained B3 and B4 candidates. The exact source, integration attempts, checks and completion verdict are in [B5 qualification](/Users/michaelfuscoletti/Desktop/beat-mario/artifacts/b5-engineering/20260926-integration/qualification-report.md). Owner acceptance and full-beta readiness remain open.

## Reconciliation and changes

B5 reuses B2 conversation, clarification, Mario revisions/variants and native handback; B3 screen-only watering and isolated setup; B4 selected-action dependencies, task ledger, observed accounting and guarded stops. The [reconciliation](/Users/michaelfuscoletti/Desktop/beat-mario/artifacts/b5-engineering/20260926-integration/reconciliation.md) maps each requirement to retained evidence and the actual remaining gap. No farm action or accepted Mario controller was broadened.

The ordinary Stardew page distinguishes B3 Day 2 all15 watering from B4 Day 5 combined work, shows the qualified display/settings and named targets, and explains setup in order. All actions remain visible after a correction. Cancel review removes Start permission. Failed Observe reports its actual reason; already-satisfied steps are labeled as pre-existing, not unfinished or executed again. The shared result history shows requests, confirmed work, remaining or uncertain work, and recovery instructions. Expand **Reopen result** to inspect; this is read-only and never restores a plan, targets or authority. Older records without the original request say so. Refused Start/review requests are also retained.

Switching now requests input release and Mario detach before selecting the next adapter. A native acknowledgment and retained history are required; unresolved handback refuses the switch. Mario conversation proposals, pending edits and revision controls are cleared along with its runtime session. Stardew clears observations, selected targets and review. Earlier messages/results remain attached to their own game as reference. New non-control requests serialize with switching; Pause, Stop and Take control retain priority. Mario process-scoped chat focus and Stardew foreground-only input are unchanged.

The current integration includes a completed Mario opening-hop edit, neutral switching, all four actual Day 5 actions, cancellation/refusal and guarded recovery, readable retained outcomes, and switching back. The current combined attempt stopped on observation freshness during return; a fresh recovery also stopped. Those attempts remain stopped, with return unconfirmed. Take control and independent OS-neutral checks passed. B4’s retained complete automatic return is reused for that unchanged game behavior; it is not relabeled as a successful current-source rerun. The later manual position at dusk could not be qualified by the daylight profile. See the exact report for every attempt and source boundary.

## Launch and use

Double-click `Open Game Companion.command`, or run `.venv/bin/python -m smb3_agent lab ui --host 127.0.0.1 --port 8765` from the repository and open `http://127.0.0.1:8765/`. The local game files, emulator, Stardew installation, frozen engineering seeds and qualified calibration/evidence must already be present; they are not bundled or fetched.

1. Choose Mario. Review a route objective. Quickest and 100% retain an explicit existing-base fallback; neither is an accepted optimized/full-completion route. Open Mario for companion play. A useful bounded request is “Use the base path and stop after the opening section.” Review and Start. “Take the opening hop, then stop after the opening section” is a supported upcoming edit. Cancel pending change cancels only unexecuted edits; it cannot undo game actions.
2. Use **Choose a game → Switch to Stardew Valley**. Wait for confirmed neutral handback. Choose **Pilot / B4Test Farm · Day 5**, open a fresh copy, Load the named farm, exit to the porch and select the watering can. Close any prior isolated Stardew copy first. **Check isolated farm session**, then connect `b4-day5-screen-v13`.
3. Type “Harvest farm--1-3, then plant parsnip seeds on farm--1-3, then water them and clear farm-0-5 and return to the farmhouse entrance.” Review all four actions, one owned seed, new-crop watering, protected neighbors, zero purchases and return. A correction such as “Leave at least 20 energy” retains the other actions: inspect the entire resulting plan. **Review scope → Start reviewed work**.
4. Keep Stardew foreground. Pause/Take control stop input. Inspect Outcome and Saved results. For partial or guarded stops, obtain a clear supported view, Observe, request only remaining work, review, then Start; if the intermediate position is unsupported, take control or open a fresh disposable copy. Refocusing never resumes permission.
5. Switch back through Games. Mario requires a fresh compatible session and reviewed plan. Saved results can be reopened without playing.

B3 uses **Pilot / B3Test · Day 2** and its matching profile, with “Water all initially planted crops.” It is a separate seed/configuration, not a substitute for Day 5.

## Limits

Stardew qualification remains the exact Standard Farm, daylight, 1512×949 capture at (0,33), 3024×1964 display, zoom75%, UI100%, locked toolbar, hit marker and WASD. B4 actions cover the selected left ordinary parsnip and small stone only. Other crops/plots, regrowth, quality/bonus yields, other debris/tools, refill, other views/settings and purchases remain unsupported. Occlusion/freshness may produce a truthful guarded stop. The original B3 all15 proof and B4 combined proof retain their identities and every failed/partial attempt remains evidence, not success.

## One B6 handoff

Run a bounded **Mario-only qualification and repair pass on the identified current source**. Reuse B5's completed opening-hop/edit/input-audit/neutral-switch evidence. Close only the remaining current-candidate Mario cases: normal→faster→normal acknowledgments and measured intervals; changed World1-1 destination; saved-variant reopen with fresh authority; manual/agent and paused reclaim; stale/late edit or lost-process recovery. Repair concrete failures and repeat only their affected paths. Select accepted-route regressions from the actual changed controller/entry contracts; do not rerun full campaigns merely for presentation or shared-history changes. Preserve quickest/100% base fallback and unknown full-completion coverage. Record exact attempts/source and return neutral player control. B6 does not reopen Stardew development or grant owner acceptance, B8 delivery readiness, or full-beta approval.
